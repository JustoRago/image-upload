# Copy this file to .env.local and adjust if your backend does not run at the
# default location. Only REACT_APP_* variables are exposed to the client build
# (Create React App). Without a value, the app falls back to
# http://localhost:5000, which matches the server's default port.
REACT_APP_API_URL=http://localhost:5000